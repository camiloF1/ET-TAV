import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'examenlogin2',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
