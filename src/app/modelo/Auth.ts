export type Auth = {
  correo: string;
  password: string;
};

export type AuthResponse = {
  id: number,
  nombre: string,
  apellido: string,
  username: string,
  edad: string,
  asignatura: string
};
